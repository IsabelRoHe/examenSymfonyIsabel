{% extends 'base.html.twig' %}

{% block title %}Estudiante{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

    <h1>Listado de estudiantes</h1>
        <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Apellido1</th>
                <th>Apellido2</th>
                <th>Número de cursos</th>
                <th>Nota Media</th>
                <th>actions</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for estudiante in estudiantes %}
            <tr>
                <td>{{ estudiante.id }}</td>
                <td>{{ estudiante.nombre }}</td>
                <td>{{ estudiante.apellido1 }}</td>
                <td>{{ estudiante.apellido2 }}</td>
                <td>{{ estudiante.getNCursos }}</td>
                <td>{{estudiante.getNotaMedia}}</td>
                <td><a href="{{ path('estudiante_notas', {'id': estudiante.id}) }}">Ver Notas</a></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
